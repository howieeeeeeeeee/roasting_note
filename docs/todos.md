### To-Dos

- [x] Add ambient temp and humidity to the roast level data (allow edit in the middle or end of roast)
- [x] Real time graph, showing the time on x-axis (baseline in 8 mins on x-axis, if time pass 8 mins, take the max time and rounf to the nearest 2 min), and show temp on theleft y-axis (max 200) and ror at right y-axis (max 50). make sure the color and design are match the current style and the display of the grpah are smooth and respnsive on frontend
- [x] Database sync/switch feature: Settings modal to switch between local/online DB, with bidirectional sync functionality (see Section 7 in req.md)
- [ ] Add test function on bean creation / roast creation / live roast data update event update / live roast end / edit roast/ add review / edit review / update bean. the test will go over all neccesary feature and check the data is correct, and maybe forntend behavrio is as expected. we will label those test data using test_data = true while create the roast and bean data. and there is a bottom called delete roast data on the db setting on the top right modal. we only run test using local db. Need the complete test fucntion and plan and also add to the CLAUDE.md of this repo, ask claude to run the basic test while their is major changes.or the user ask them to, add insturciton their.
